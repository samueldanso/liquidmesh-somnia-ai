services:
    - type: web
      name: liquidmesh-agents
      runtime: bun
      rootDir: agents
      buildCommand: bun install
      startCommand: bun start
      envVars:
          - key: NODE_ENV
            value: production
          - key: PORT
            value: 8000
